# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

exec set_workspace_name.sh 1 "1: Term"
exec set_workspace_name.sh 2 "2: Slack"
exec set_workspace_name.sh 3 "3: Thunderbird"
exec set_workspace_name.sh 4 "4: Chrome"
exec set_workspace_name.sh 5
exec set_workspace_name.sh 6
exec set_workspace_name.sh 7
exec set_workspace_name.sh 8
exec set_workspace_name.sh 9
exec set_workspace_name.sh 10 "0: Scratch"
exec set_workspace_name.sh 11 "a"
exec set_workspace_name.sh 12 "b"
exec set_workspace_name.sh 13 "c"
exec set_workspace_name.sh 14 "d"
exec set_workspace_name.sh 15 "e"
exec set_workspace_name.sh 16 "f"
exec set_workspace_name.sh 17 "g"

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec lilyterm

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+x exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+v split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+m fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+Shift+t layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 exec "call_wp_i3msg.sh 1 'workspace $WP'"
bindsym $mod+2 exec "call_wp_i3msg.sh 2 'workspace $WP'"
bindsym $mod+3 exec "call_wp_i3msg.sh 3 'workspace $WP'"
bindsym $mod+4 exec "call_wp_i3msg.sh 4 'workspace $WP'"
bindsym $mod+5 exec "call_wp_i3msg.sh 5 'workspace $WP'"
bindsym $mod+6 exec "call_wp_i3msg.sh 6 'workspace $WP'"
bindsym $mod+7 exec "call_wp_i3msg.sh 7 'workspace $WP'"
bindsym $mod+8 exec "call_wp_i3msg.sh 8 'workspace $WP'"
bindsym $mod+9 exec "call_wp_i3msg.sh 9 'workspace $WP'"
bindsym $mod+0 exec "call_wp_i3msg.sh 10 'workspace $WP'"
bindsym $mod+a exec "call_wp_i3msg.sh 11 'workspace $WP'"
bindsym $mod+b exec "call_wp_i3msg.sh 12 'workspace $WP'"
bindsym $mod+c exec "call_wp_i3msg.sh 13 'workspace $WP'"
bindsym $mod+d exec "call_wp_i3msg.sh 14 'workspace $WP'"
bindsym $mod+e exec "call_wp_i3msg.sh 15 'workspace $WP'"
bindsym $mod+f exec "call_wp_i3msg.sh 16 'workspace $WP'"
bindsym $mod+g exec "call_wp_i3msg.sh 17 'workspace $WP'"

# move focused container to workspace
bindsym $mod+Shift+1 exec "call_wp_i3msg.sh 1 'move container to workspace $WP'"
bindsym $mod+Shift+2 exec "call_wp_i3msg.sh 2 'move container to workspace $WP'"
bindsym $mod+Shift+3 exec "call_wp_i3msg.sh 3 'move container to workspace $WP'"
bindsym $mod+Shift+4 exec "call_wp_i3msg.sh 4 'move container to workspace $WP'"
bindsym $mod+Shift+5 exec "call_wp_i3msg.sh 5 'move container to workspace $WP'"
bindsym $mod+Shift+6 exec "call_wp_i3msg.sh 6 'move container to workspace $WP'"
bindsym $mod+Shift+7 exec "call_wp_i3msg.sh 7 'move container to workspace $WP'"
bindsym $mod+Shift+8 exec "call_wp_i3msg.sh 8 'move container to workspace $WP'"
bindsym $mod+Shift+9 exec "call_wp_i3msg.sh 9 'move container to workspace $WP'"
bindsym $mod+Shift+0 exec "call_wp_i3msg.sh 10 'move container to workspace $WP'"
bindsym $mod+Shift+a exec "call_wp_i3msg.sh 11 'move container to workspace $WP'"
bindsym $mod+Shift+b exec "call_wp_i3msg.sh 12 'move container to workspace $WP'"
bindsym $mod+Shift+c exec "call_wp_i3msg.sh 13 'move container to workspace $WP'"
bindsym $mod+Shift+d exec "call_wp_i3msg.sh 14 'move container to workspace $WP'"
bindsym $mod+Shift+e exec "call_wp_i3msg.sh 15 'move container to workspace $WP'"
bindsym $mod+Shift+f exec "call_wp_i3msg.sh 16 'move container to workspace $WP'"
bindsym $mod+Shift+g exec "call_wp_i3msg.sh 17 'move container to workspace $WP'"

# reload the configuration file
#bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Control+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command my_status.sh
        tray_output primary
}

# Anthony's changes

no_focus [window_role="pop-up"]
focus_follows_mouse no
mouse_warping none
popup_during_fullscreen smart
#New syntax: focus_wrapping force
force_focus_wrapping yes
workspace_auto_back_and_forth yes
force_display_urgency_hint 500 ms
focus_on_window_activation urgent
exec xrandr --output DP-1.1 --primary --auto --right-of eDP-1-1 --output DP-1.3 --auto --right-of DP-1.1

exec call_wp_i3msg.sh 2 'workspace $WP; exec slack;'
exec call_wp_i3msg.sh 3 'workspace $WP; exec thunderbird;'
exec call_wp_i3msg.sh 4 'workspace $WP; exec google-chrome;'
bindsym $mod+z exec i3lock -f -e -c 202020
exec lilyterm
exec nm-applet
exec pasystray
exec xfce4-power-manager
exec blueman-applet

# move current workspace between monitors
bindsym $mod+Control+h move workspace to output left
bindsym $mod+Control+l move workspace to output right

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume 0 +5% #increase sound volume
bindsym XF86AudioLowerVolume exec pactl set-sink-volume 0 -5% #decrease sound volume
bindsym XF86AudioMute exec pactl set-sink-mute 0 toggle # mute sound

# Media player controls
bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous
#
# Sreen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

bindsym $mod+Control+r exec i3-input -F 'exec rename_workspace.sh "%s"' -P 'New workspace name: '

#TODO: Mark / Unmark:
## read 1 character and mark the current window with this character
#bindsym $mod+m exec i3-input -F 'mark %s' -l 1 -P 'Mark: '
# Get current workspace's number
# i3-msg -t get_workspaces | jq '.[] | select(.focused==true).name'

# read 1 character and go to the window with the character
#bindsym $mod+g exec i3-input -F '[con_mark="%s"] focus' -l 1 -P 'Goto: '
